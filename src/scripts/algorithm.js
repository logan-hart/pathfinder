const Node = require("./node.js");
const Path = require("./path.js");
const Graph = require("./graph.js")

function Algorithm(graph){
    this.graph = graph
}

function animateGraph(graph){
    // let unvistedNodes = []
    // let vistiedNodes = []
    // let distance =
    // let 


}

function buildAssociations(graph){
    // let associations = {}
    // for (let i = 0; i < this.graph.nodes.length; i++){
    //     if (!associations[i]) associations[i] = {} 
    //     for (let j = 0; j < this.graph.nodes.length; j++)
    //         associations[i].
    // }
    // console.log(associations)
}

// let associations = {
    // 	node_Number0 { neighbor_1: travel_weight, neighbor_2: travel_weight },
    // 	A: { start: 1, C: 4, D: 2 },
    // 	B: { A: 8, D: 7 },
    // 	C: { D: 6, finish: 3 },
    // 	D: { finish: 1 },
    // 	finish: {},
// };

module.exports = Algorithm;

